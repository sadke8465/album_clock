{
  "name": "albumclock-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on Cloudflare Worker that serves the current 64x64 RGB565 album-art frame for the Waveshare album clock.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cf-wasm/photon": "^0.1.27"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
